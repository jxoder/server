/**
 * @storybook
 * https://adminjs-storybook.web.app
 */
export {
  Input,
  Label,
  Text,
  Box,
  H1,
  Card,
  Button,
  // @ts-ignore 타입 오류가 발생되는데 runtime 에서 동작함.
} from '@adminjs/design-system'
